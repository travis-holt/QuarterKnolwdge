node_modules
dist
.vite
*.local
migration-bundles/

# Playwright e2e artifacts (browsers install to a user-level cache, not here)
test-results/
playwright-report/
blob-report/
.playwright/

# Agent tooling (ponytail plugin) — not part of the app
.agents/
.claude-plugin/
skills/
gemini-extension.json

# Local agent workspace/config — machine-specific, never commit
.codex-worktrees/
.mcp.json

# Generic secret patterns — a future .env or key/credential file must never
# land in this public repo (only .env.local was covered before, via *.local)
.env
.env.*
!.env.local.example
*.pem
*.key
*.p12
service-account*.json

# Sensitive — never commit to this public repo (live API key / patient PII)
roo-code-settings.json
OB GYN SOP.pdf
Pediatrics_SOP_Updated.pdf
Aizer_Health_Navigator_Quality_Guide_SOP.pdf
Knowledge Check Webapp Bugs And Feature Tweaks.docx

# Firebase CLI debug log
firebase-debug.log
